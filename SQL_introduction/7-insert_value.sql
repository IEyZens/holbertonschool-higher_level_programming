-- Insert a new row into 'first_table' with:
--   - id = 89
--   - name = 'Best School'
-- The other columns ('c' and 'created_at') will be set to NULL unless defaults are defined
INSERT INTO first_table (id, name) VALUES (89, 'Best School')
